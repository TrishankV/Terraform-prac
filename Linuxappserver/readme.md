This is using the same windows web server we created but her ewe are mapping it to a linux app server where we can make static web applications which could indeed be  hosted on the web server of windows . Well that's what i understood .

Here we create a map of objects for subnets and these subnets are looped to automate the creation and managing of Windows and Linux webservers insdied the subnets 
